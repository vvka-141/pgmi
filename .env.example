# pgmi Environment Variables
# Copy this file to .env for local development (optional - you can also export these directly)

# ============================================================================
# CONNECTION OPTIONS (choose one approach)
# ============================================================================

# Option 1: pgmi-specific connection string (legacy, still supported)
# PGMI_CONNECTION_STRING=postgresql://postgres:postgres@localhost:5433/postgres

# Option 2: DATABASE_URL (Heroku, Rails, cloud platforms)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5433/postgres

# Option 3: PostgreSQL standard environment variables (recommended)
# These work with all PostgreSQL tools (psql, pg_dump, pg_restore, pgmi)
PGHOST=localhost
PGPORT=5433
PGUSER=postgres
# PGPASSWORD=postgres  # Not recommended - use .pgpass file instead
PGDATABASE=postgres
# PGSSLMODE=prefer     # Options: disable|allow|prefer|require|verify-ca|verify-full

# ============================================================================
# NOTES
# ============================================================================
# - CLI flags always override environment variables
# - Precedence: CLI flags > env vars > defaults
# - The database in the connection string is used for CREATE DATABASE operations
# - For passwords, prefer using .pgpass file or connection string over PGPASSWORD
# - You can mix and match (e.g., use PGHOST but override with --port flag)
